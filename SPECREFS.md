# Pinned specification versions

| Spec | Version pinned |
|---|---|
| IETF Token Status List | draft-ietf-oauth-status-list-08 (**CONFIRM & vendor — not in references/**) |
| JWS | RFC 7515 (via go-eudi-crypto) |
| COSE_Sign1 / CWT | RFC 9052 / RFC 8392 (via go-eudi-crypto) |
| zlib / DEFLATE | RFC 1950 / RFC 1951 |
| ARF Topic 7 revocation | ARF 2.9 §6.6.3.7; annex-2.02 HLRs VCR_01, VCR_02, VCR_11, VCR_13 |
| Short-lived exemption origin | ETSI EN 319 411-1 v1.4.1 REV-6.2.4-03A (24h) |

## Open items (flagged to maintainers)
- draft-ietf-oauth-status-list is not vendored under `references/`. Confirm the
  exact draft version and add it. Verify the CWT claim keys used in `cwt.go`
  (`status_list`=65533, `ttl`=65534) and the CWT `typ` label (16 =
  "application/statuslist+cwt") against the pinned draft.
- The Attestation Revocation List ("Identifier List") wire format is defined in
  the Commission TS referenced by ARF VCR_11, which is not vendored. The
  JSON/CBOR shape in `identifier.go` is a documented synthetic choice
  (ADR-0007). Verify against that TS before v1.
- All test vectors are generated by an in-test issuer (ADR-0007). Replace with
  draft appendix vectors if the vendored draft ships them.
